- name: play level variables
  host: localhost
  connections: local 
  vars:
    COURSE: ansible
    DURATION: 7 hrs
    LEARNER: amara
  tasks:
    -name : print the course info
    ansible.builtin.debug:
    msg: "course Name is:{{COURSE}},duration: {{DURATION}},learner: {{LEARNER}}"