- name: vriables from files
  host: local
  connection: local connection
  vars_files:
  - course.yaml
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "Course Name is:{{ COURSE }}, Duration: {{ DURATION }}, Trainer {{ TRAINER }} "